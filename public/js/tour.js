introJs().addHints();
introJs().setOptions({
    showProgress: true,
    showBullets: false,
    dontShowAgain: true,
    exitOnOverlayClick: false,
    scrollToTooltip: true,
    scrollTo: 'tooltip',
    tooltipPosition: 'bottom-middle-aligned',
}).start();